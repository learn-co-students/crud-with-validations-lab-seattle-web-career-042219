<h3><%= @song.title %></h3>
by <b><%= @song.artist_name %></b>
<% if @song.released? %>
  <p>
    Released in
    <b><%= @song.release_year %></b>
  </p>
<% end %>
<p>
  <b>Genre:</b>
  <%= @song.genre %>
</p>

<%= link_to 'Edit', edit_song_path(@song) %>